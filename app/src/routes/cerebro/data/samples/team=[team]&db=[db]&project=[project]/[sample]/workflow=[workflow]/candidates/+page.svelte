<script lang="ts">
	import { page } from "$app/stores";

	// import QualityControl from "$lib/components/data/sample/QualityControl.svelte";
	// import type { Cerebro, QualityControlSummary, WorkflowConfig } from "$lib/utils/types";

    // export let selectedWorkflowConfiguration: WorkflowConfig;
    // export let selectedModels: Cerebro[] = $page.data.sampleCerebro?.length ? [$page.data.sampleCerebro[0]] : [];    
    // export let selectedQualityControlSummaries: QualityControlSummary[] = $page.data.qualityControlSummaries?.length ? $page.data.qualityControlSummaries[0] : [];


</script>

<div class="w-full mb-5">
    <p class="mb-1"><span class="opacity-60">Candidate Selection</span></p>
    <div class="mb-4 border border-primary-500 p-8 rounded-md">
            
    </div>
</div>
